<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sc703.etiquetas_kotlin">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CALL_PHONE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Etiquetas_kotlin"
        android:usesCleartextTraffic="true">
        <!--Esto esta dando error, SalirActivity no existe, no sé si alguno lo dejó con algún propósito-->
        <!--<activity android:name=".ui.salir.SalirActivity"></activity>-->
        <activity android:name=".RegistrarActivity" />
        <activity android:name=".RecuperarPassActivity" />
        <activity android:name=".MainActivity" />
        <activity
            android:name=".ActivityPrincipal"
            android:label="@string/title_activity_principal"
            android:theme="@style/Theme.Etiquetas_kotlin.NoActionBar" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-8276976963027963~6570321474"/>

    </application>

</manifest>